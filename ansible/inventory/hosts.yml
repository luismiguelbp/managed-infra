---
all:
  children:
    # Active template examples.
    linux_hosts:
      hosts:
        edge-node-1:
          ansible_host: edge-node-1.example.lan
        edge-node-2:
          ansible_host: edge-node-2.example.lan
    # Standby template example.
    linux_hosts_standby:
      hosts:
        edge-node-3:
          ansible_host: edge-node-3.example.lan
